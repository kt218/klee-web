/**
 * Definition for singly-linked list.
 * typedef struct Node {
 *     int x;
 *     struct Node *next;
 * } node;
 */

int detect_cycle(node* head) {
  // Using the above definition for a singly-linked list
  // implement a function that detects a cycle within the list
  // and returns the index of the element where the cycle starts
  // It should return -1 if no cycle is detected
}